<% if @answer.errors.present? %>
	<% @answer.errors.full_messages.each do |message| %>
		$('.form-errors').html('<div class="alert-box alert"><%= j message %></div>');
	<% end %>
<% else %>
	$('.answers').html('<%= j render 'questions/answers' %>');
	$('#answer_body').val('');
<% end %>
